#!/bin/bash

build_dir="$(dirname "$0")" || exit $?;
build_dir="${build_dir:-.}";
cd "$build_dir/.." || exit $?;

config=config.debug;

if [ ! -f "$config" ]; then
    echo "'$config' not found." 1>&2;
    exit 1;
fi;

if [ "$1" == -f ]; then
    ./debug/install "$@";
fi;

bambot -F"$config";
